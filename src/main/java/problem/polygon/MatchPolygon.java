package problem.polygon;

public class MatchPolygon{

    public int countPolygon(int[][] input) {
        // n * n 배열 ( n <= 4 )
        // -1 <= f(n) <= 4
        // f(n)은 주변에 놓는 성냥개비 갯수를 의미
        // 하나의 다각형이 되는 경우의 수

        // 1 * 1 인 경우 -1 or 4 일 때 다각형 수 한개 나머지는 0
        // 2 * 2 인 경우 다각형의 경우는 총 13개
        // 3 * 3 인 경우
        // 4 * 4 인 경우





        return 0;
    }
}
